# Copier ce fichier vers .env (à la racine, à côté de celui-ci) et compléter.
# .env n'est jamais commité (voir .gitignore) — chacun a le sien en local.

POSTGRES_USER=journal
POSTGRES_PASSWORD=changez_moi
POSTGRES_DB=journal_de_bord
